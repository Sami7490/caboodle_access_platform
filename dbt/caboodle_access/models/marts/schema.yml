version: 2

models:
  - name: dim_patients
    columns:
      - name: patient_key
        tests:
          - unique
          - not_null

  - name: fact_encounters
    columns:
      - name: encounter_key
        tests:
          - unique
          - not_null
      - name: patient_key
        tests:
          - not_null

  - name: fact_appointments
    columns:
      - name: appointment_key
        tests:
          - unique
          - not_null
      - name: patient_key
        tests:
          - not_null

  - name: fct_readmissions
    columns:
      - name: encounter_key
        tests:
          - unique
          - not_null
